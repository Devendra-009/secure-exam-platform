{
  "name": "secure-exam-platform",
  "version": "3.1.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\" \"npm run dev --prefix electron-app\"",
    "start": "npm run start --prefix server",
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix electron-app",
    "seed": "npm run seed --prefix server",
    "build": "npm run build --prefix client",
    "validate": "node scripts/validate.mjs",
    "migrate": "npm run migrate --prefix server"
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  },
  "engines": {
    "node": ">=22 <25"
  }
}
